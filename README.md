# octo-cli PM Agent Lab

This repository is the public demand pool and PM workflow workspace for the AINOL Agent practical exam.

Target product:

https://github.com/Mininglamp-OSS/octo-cli

Important rules:

- The target repository is read-only.
- This repository stores issues, PRDs, workflows, labels, cron logs, and exam documentation.
- Product answers must cite verifiable source paths and line numbers.
- Secrets must never be written to issues, PRDs, logs, or chat messages.
- Cron runs automatically and records logs.
- Group reports are sent only when there is actual output.
